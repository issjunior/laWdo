name: CI

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validar:
    name: Validar
    runs-on: windows-latest
    steps:
      - name: Obter código
        uses: actions/checkout@v7.0.1

      - name: Configurar Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Instalar dependências
        run: npm ci

      - name: Verificar tipos
        run: npm run type-check

      - name: Executar lint
        run: npm run lint

      - name: Executar testes com cobertura
        run: npm run test:coverage

      - name: Executar testes de release
        run: npm run test:release

      - name: Validar contrato de schema para release
        run: node scripts/release/validar-schema-release.mjs

      - name: Gerar build
        run: npm run build

      - name: Executar smoke de schema
        run: npm run smoke:schema
